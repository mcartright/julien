package julien
package retrieval

import org.scalatest._
import org.scalamock.scalatest.proxy.MockFactory
import julien._

class UnorderedWindowSpec extends FlatSpec with MockFactory {
  "An unordered window" should
  "require all children to be from the same index" in (pending)

  it should "complain if given a window size < 1" in (pending)

  it should "correctly count the number of windows" in (pending)
}
