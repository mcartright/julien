package julien
package retrieval

import org.scalatest._

class SimpleProcessorSpec extends FlatSpec {
  "The SimpleProcessor" should "accept operators" in (pending)
  it should "accept indexes" in (pending)
  it should "complain if more than 1 index is provided" in (pending)
  it should "automatically attach hooks if only 1 index is provided" in (pending)
  it should "iterate over and score every candidate document (stupidly)" in (pending)
}
