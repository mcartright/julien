package julien
package retrieval

import org.scalatest._
import org.scalamock.scalatest.proxy.MockFactory

class WeightSpec extends FlatSpec with MockFactory {
  "A weight operator" should "multiply its given feature" in (pending)
}
