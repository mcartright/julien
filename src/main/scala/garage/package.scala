/** Top-level package for modeling attempts at Julien that
  * never gained traction. Kept around for posterity and
  * differing points of view when thinking about how to abstract
  * indexing, retrieval, and system interaction. Or at least I like
  * to think so.
  */
package object garage {
}
